# Demostración: comparador de tratamientos

Este prototipo es independiente de la aplicación principal. No modifica sus rutas,
menús ni pantallas.

Para verlo en Flutter Web, desde la carpeta `tao`:

```bash
flutter run -d chrome -t prueba/main.dart
```

Para descartarlo por completo, elimina la carpeta `prueba`.
